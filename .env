# CashFlow402 — local dev environment
# Rename this to .env before running npm run dev

PORT=3000
NODE_ENV=development

# JWT
JWT_SECRET=dev_jwt_secret_replace_before_production
JWT_EXPIRY_PERCALL=60
JWT_EXPIRY_SUBSCRIPTION=3600

# BCH Network
BCH_NETWORK=chipnet

# Electrum (ChipNet)
ELECTRUM_HOST=chipnet.imaginary.cash
ELECTRUM_PORT=50004
ELECTRUM_PROTOCOL=wss

# Merchant Wallet (dev/test — replace with real ChipNet key)
# Generate: node -e "import('@bitauth/libauth').then(({generatePrivateKey,encodePrivateKeyWif})=>{const k=generatePrivateKey(()=>require('node:crypto').getRandomValues(new Uint8Array(32)));console.log(encodePrivateKeyWif(k,'testnet'))})"
MERCHANT_WIF=cPWEtAdKVCSNiAvpqWXwqFbJCnPE3xWQbFsu3qcAMXYotVSowHJQ
MERCHANT_ADDRESS=bchtest:qr5a6yya7wez6g77q07rwnu4twuzc7hpvy4d6wwzsk

# Token
SUBSCRIPTION_TOKEN_CATEGORY=

# Rates
DEFAULT_PERCALL_RATE_SATS=546
DEFAULT_INTERVAL_BLOCKS=1
DEFAULT_AUTHORIZED_SATS=2000

# Webhook
WEBHOOK_SECRET=dev_webhook_secret

# ChipNet E2E script
API_URL=http://localhost:3000
# SUBSCRIBER_WIF=   ← optional, generated fresh each run if unset
